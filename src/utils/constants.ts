const storageKeys = {
  THEME: 'theme',
  ENV: 'environment',
};

const environments = {
  windows: 'windows',
  terminal: 'terminal',
};

const themes = {
  light: 'light',
  dark: 'dark',
};

export {
  storageKeys,
  themes,
  environments,
};